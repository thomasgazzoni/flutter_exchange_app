import 'package:flutter/material.dart';

class UiConfig {
  // names
  static const String appName = "Flutter Exchange App";

  // fonts
  static const String fontFamily = "Quicksand";

  // styles
  static final appBarTextStyle =
      TextStyle(fontFamily: 'Quicksand', fontSize: 25);

// sizes
  static const MARKET_COLUMN_SIZE = [0.46, 0.30, 0.24];
}
